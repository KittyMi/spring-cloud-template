feign:
  httpclient:
    enabled: false
  okhttp:
    enabled: true

zipkin:
  base-url: http://zipkin
  boot:
    admin:
      client:
        url: http://zipkin:9003

# 暴露监控端点
management:
  endpoints:
    web:
      exposure:
        include: '*'